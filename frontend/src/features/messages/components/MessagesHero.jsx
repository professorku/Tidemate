import SectionShell from '../../../components/ui/SectionShell'
import SectionHeader from '../../../components/ui/SectionHeader'

export default function MessagesHero() {
  return (
    <SectionShell padding="hero">
      <SectionHeader
        title="Messages"
        description="Manage conversations with hosts and renters, follow up on booking requests, and keep all trip communication in one place."
      />
    </SectionShell>
  )
}
